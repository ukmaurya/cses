#include<bits/stdc++.h>
using namespace std;
int main(){
   /* int n ;       fails on AA__   ans __AA   if we put  AA   string becomes AAAA and  AAAA which is same but counted twice 
    cin>>n;
    string pat ;
    cin>>pat;
    int mod = 1e9+7;
    int m = pat.size();
   int lenDiff = n-m;
   int ans = 0;
   vector<long long > power(1000);
   power[0]=1;
   for(int i=1;i<1000;i++){
    power[i]= (power[i-1]*26)%mod;
   }
 
   while(lenDiff+1){
      long long temp = power[n-m]%mod;
      ans =(ans+temp)%mod;
      lenDiff--;
   }
   cout<<ans<<endl;
   return 0; */  

}